import styled from 'styled-components'

const Button = styled.button`
  padding-top: 8px;
  display: flex;
  align-items: center;
  background-color: transparent;
  border: none;

  & svg {
    margin-right: 4px;
  }
`

export {
  Button
}
